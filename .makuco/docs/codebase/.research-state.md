# Research state — catdog

Working state of `makuco-project-research` for this repository.

```yaml
repo: '.'
commit: 2b1bae2
pass: 1
inventory:
  tracked_files: 388
  top_dirs: ['.claude', 'services', '.makuco', 'repos']
  extensions: ['ts', 'tsx', 'json', 'md', 'svg', 'mjs', 'sql']
  declared_units: ['services/backend', 'services/frontend']
  units_source: 'listagem (sem workspaces/monorepo tooling formal)'
done: ['stack.md', 'structure.md', 'architecture.md', 'conventions.md', 'integrations.md', 'concerns.md', 'OVERVIEW.md']
adopted: ['testing.md']
pending: []
modules:
  done: ['autenticacao-e-autorizacao', 'registro-de-animais', 'especies', 'admin-scaffolding']
  pending: []
  skipped:
    - 'supabase (provider): infraestrutura compartilhada, sem capacidade de negócio própria — uma linha no mapa de módulos, sem doc'
    - 'ui (components/ui): primitivas shadcn/ui geradas via CLI, sem lógica própria — uma linha no mapa de módulos, sem doc'
claims_pending: []
index_updated: true
caps_hit: []
```

## Notas

- `testing.md` já existia (escrito por `makuco-documentation` em 2026-09-11, atualizado com a nota sobre Radix UI travando em jsdom) — adotado sem re-derivação, avaliado como fresco e correto.
- Workspace single-repo/legado (`repos/` não contém sub-diretório de repositório real, só `repos/README.md`) — `MAKUCO.md` não tem índice multi-repo a atualizar; `index_updated: true` reflete que essa etapa não se aplica, não que algo foi escrito nele.
- `research.referencias: []` já estava configurado em `MAKUCO.md` — nenhuma reconciliação de doc de referência do time necessária.
- Rodada inteira (Steps 0-11) executada inline nesta mesma sessão (156 arquivos de aplicação, abaixo do limiar de 200 que aciona delegação a sub-agentes).
