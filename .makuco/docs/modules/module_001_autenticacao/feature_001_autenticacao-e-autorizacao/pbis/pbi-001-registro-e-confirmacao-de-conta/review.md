---
stage: review
feature: autenticacao-e-autorizacao
pbi: registro-e-confirmacao-de-conta
created_at: 2026-09-07
status: done
---

# Review — Registro e confirmação de conta

## Rodada de revisão 1 — 2026-09-07

**Veredicto:** NECESSITA CORREÇÕES
**Tasks revisadas:** task-01, task-02, task-03, task-04, task-05, task-06, task-07, task-08, task-09, task-10

### Resumo

Revisão completa (modo PBI, rodada 1) dos 23 arquivos de código desta PBI — 7 passes executadas (fan-out completo: 5 subagentes em paralelo para as passes 3-7, mais passes 1-2 no orquestrador), 18 achados após dedup/verificação (1 critical, 1 major, 10 minor, 6 suggestion), a partir de 23 candidatos brutos. Veredicto NECESSITA CORREÇÕES por 1 achado crítico (CORS ausente — impede o fluxo de cadastro de funcionar de verdade em um navegador real) e 1 achado major (endpoint público de cadastro sem rate limiting).

### Achados

| # | Severidade | Arquivo | Linha | Categoria | Descrição | Recomendação |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | critical | `services/backend/src/main.ts` | L5-9 | boundary/CORS | **CONFIRMED.** `bootstrap()` nunca chama `app.enableCors()`, e não há proxy/rewrite em `services/frontend/next.config.ts` (verificado — arquivo vazio, sem `rewrites`). O frontend chama `fetch` direto para `http://localhost:3001` a partir da origem do Next.js (porta diferente). Qualquer usuário real abrindo a aplicação no navegador tem a requisição de registro bloqueada pela política de CORS — o fluxo de cadastro (REG-01 a REG-09) nunca funciona fora dos testes automatizados, porque nem Supertest (chama o servidor Nest direto) nem o `fetch` mockado nos testes de frontend passam pela política de CORS de um navegador real. | Adicionar `app.enableCors({ origin: '<origem do frontend>' })` em `main.ts`, antes de `app.listen`. |
| 2 | major | `services/backend/src/auth/auth.controller.ts` | L29-31 | segurança (OWASP A04) | **CONFIRMED.** `POST /auth/register` é público, sem nenhum rate limiting (`ThrottlerModule` ausente de `app.module.ts`, confirmado). Um script simples pode criar contas em massa, sondar e-mails já cadastrados via respostas 409 repetidas, e esgotar a quota da Admin API do Supabase. | Adicionar `@nestjs/throttler` (ou guard equivalente) com limite agressivo neste endpoint. |
| 3 | minor | `services/backend/src/auth/use-cases/register.use-case.ts` | L98-103 | robustez / contrato externo | **PLAUSIBLE** (rebaixado de major na verificação — depende de uma mudança futura da API do Supabase, não é um bug hoje). Detecção de e-mail duplicado usa `error.code === 'email_exists'` com fallback por regex na mensagem. Corresponde ao contrato documentado do `@supabase/auth-js` hoje; se a mensagem mudar (localização, nova versão do GoTrue) e `code` vier ausente, um cadastro duplicado passaria a cair em 500 genérico em vez de 409 (REG-05). | Preferir só `error.code` como critério primário; se manter o regex como fallback, logar quando ele for o único critério que bateu, para detectar esse drift em produção. |
| 4 | minor | `services/frontend/src/lib/api/auth.ts` | L48-64 | contrato / tratamento de erro | **CONFIRMED, mas sem consequência visível hoje** (rebaixado de major — verificado que `register-form.tsx` L100-105 só lê `error.status === 409` e ignora `error.message` para qualquer outro status, então o bug é inerte no fluxo atual). `extractErrorMessage` só aceita `message` como `string`; o `ValidationPipe` do Nest retorna `message` como **array** em erros 400 de validação. A função cai no fallback genérico nesse caso. Vira um problema real assim que outra tela passar a exibir `error.message` diretamente. | Tratar `message: string[]` explicitamente (`Array.isArray(message) ? message.join(', ') : message`). |
| 5 | minor | `services/backend/src/auth/dto/register.dto.ts` | L5-13 | validação de entrada | Sem `@MaxLength` em `nome`/`senha` (payloads arbitrariamente grandes chegam à Admin API do Supabase) e `nome` aceita string só de espaços (`@IsNotEmpty` não rejeita `"   "`). | Adicionar `@MaxLength` (ex.: 72 em `senha`, compatível com bcrypt; 100-200 em `nome`) e `@Matches(/\S/)` ou `trim()` em `nome`. |
| 6 | minor | `services/backend/src/main.ts` | L7 | segurança (OWASP A05) | `ValidationPipe` global sem `whitelist`/`forbidNonWhitelisted`/`transform`. Hoje inofensivo porque o use case só lê campos explícitos do DTO, mas é uma lacuna de defesa em profundidade. | `new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true })`. |
| 7 | minor | `services/backend/src/auth/use-cases/register.use-case.ts` | L53-93 | observabilidade (OWASP A09) | Nenhum evento de segurança é logado (sucesso de cadastro, tentativa de e-mail duplicado, erro do Supabase) — sem trilha para detectar abuso ou diagnosticar falha em produção. | Logar os eventos relevantes (sem PII sensível/senha), preservando as mensagens genéricas já retornadas ao cliente. |
| 8 | minor | `services/backend/src/main.ts` (L8) vs `services/frontend/src/lib/api/auth.ts` (L11) | — | configuração | Fallback de porta do backend é `3000`; fallback de URL do frontend é `http://localhost:3001`. `.env.example` documenta `PORT=3001`, mas um clone novo sem copiar `.env` primeiro sobe os dois serviços apontando para lugares incoerentes entre si. | Alinhar os fallbacks, ou falhar cedo (erro no boot) se `PORT` não estiver definido. |
| 9 | minor | `services/backend/src/auth/dto/match.validator.ts` | L21 | consistência de idioma | Mensagem de validação padrão em inglês ("must match {campo}") — a única do módulo que não está em PT-BR. | Traduzir para "deve corresponder a {campo}". |
| 10 | minor | `services/backend/src/auth/use-cases/register.use-case.ts` (L53-58) e `services/frontend/src/app/registro/confirmacao-pendente/page.tsx` (L47-51) | — | Object Calisthenics (nível de indentação) | `if` aninhado em ambos os arquivos (tratamento de erro do Supabase; limpeza do intervalo de cooldown) — dois níveis de indentação onde um bastaria. | Achatar com guard clauses/early return em cada um. |
| 11 | minor | `services/backend/src/auth/use-cases/register.use-case.ts` (L11), `services/frontend/src/lib/api/auth.ts` (L22), `services/backend/src/auth/dto/register.dto.ts` (campos) | — | linguagem ubíqua | `UserRole = 'admin' \| 'adotante'` mistura inglês/português no mesmo tipo, duplicado (sem contrato compartilhado) entre backend e frontend; `RegisterDto`/`RegisteredUser` também mesclam campos PT (`nome`, `senha`) e EN (`email`, `id`, `role`) na mesma classe. | Padronizar o idioma dos termos de domínio; avaliar extrair um contrato de tipos compartilhado entre backend/frontend numa PBI futura. |
| 12 | minor | `services/frontend/src/app/registro/confirmacao-pendente/page.tsx` | L56 | clareza | Literal `1000` (ms) no `setInterval` não é nomeado, ao contrário de `RESEND_COOLDOWN_SECONDS`, já extraído. | Extrair para uma constante `ONE_SECOND_IN_MILLISECONDS`. |
| 13 | suggestion | `services/backend/src/auth/use-cases/register.use-case.ts` | L42 | SOLID-D | `RegisterUseCase` depende diretamente do SDK concreto do Supabase e do formato de erro específico dele. | Extrair uma abstração de domínio (ex.: `AuthUserGateway`) injetada, isolando o caso de uso do provedor de autenticação. |
| 14 | suggestion | `services/frontend/src/components/auth/register-form.tsx` | L134-234 | duplicação | Quatro blocos `Controller`+`Field`+`Input`+`FieldError` (nome/email/senha/confirmarSenha) repetem a mesma estrutura com pequenas variações. | Extrair um componente `RegisterFormField` parametrizado por nome/label/tipo/autoComplete. |
| 15 | suggestion | `services/frontend/src/components/auth/register-form.tsx` | L85-88 | tratamento de erro | Caminho `onSubmit` (usado principalmente em testes) não tem `try/catch`, diferente do caminho padrão via `registerUser`. | Envolver também essa chamada em `try/catch`, atualizando `submitError` em caso de falha. |
| 16 | suggestion | `services/frontend/src/app/registro/confirmacao-pendente/page.tsx` | L17 | robustez de UI | Acesso direto sem `?email=` na URL mostra "Enviamos um e-mail de confirmação para ." sem endereço algum. | Mostrar mensagem genérica sem o placeholder, ou redirecionar para `/registro`, quando `email` vier vazio. |
| 17 | suggestion | `services/backend/src/auth/use-cases/register.use-case.ts` | — | funcionalidade futura | Supabase-js expõe `auth.resend({type:'signup', email})` — o botão de reenvio (task-10) hoje só reinicia o cooldown visual, sem chamada real. | Avaliar implementar um endpoint real de reenvio usando esse método nativo, quando essa necessidade entrar em escopo de uma feature futura. |
| 18 | suggestion | `services/backend/src/main.ts` | L5-9 | hardening | Sem `helmet()` configurado no bootstrap da API. | Adicionar como hardening de baseline (não bloqueante para esta PBI). |

### Cobertura dos critérios de aceite

| Critério | Task | Status |
| --- | --- | --- |
| REG-01 | task-01, task-08 | Verificado |
| REG-02 | task-01, task-02, task-03, task-04, task-06, task-09 | Verificado |
| REG-03 | task-06, task-07 | Verificado |
| REG-04 | task-07 | Verificado |
| REG-05 | task-06, task-07, task-09 | Verificado |
| REG-06 | task-08 | Verificado |
| REG-07 | task-05, task-08 | Verificado |
| REG-08 | task-06 | Verificado |
| REG-09 | task-07, task-10 | Parcialmente implementado — divergência declarada no código (`[NOTA]`), motivo técnico válido (Supabase Admin API não expõe status de envio de e-mail); não bloqueia por si só |

### Notas de escopo

- Fora de escopo de achado: scaffold gerado por `create-next-app`/`nest new` (controllers/páginas padrão, arquivos de configuração), manifestos de dependência (`package.json`/lock), e as 8 primitivas geradas pelo shadcn CLI (`services/frontend/src/components/ui/**`) — seriam sobrescritas no próximo `npx shadcn add`.
- 2 candidatos descartados por não serem achados (não entram na tabela): política de senha além do mínimo de 8 caracteres (Pass 5) já está dentro do escopo definido por REG-07, não é uma violação; reformatação cosmética incidental de `match.validator.ts` misturada num commit de fix — estilo que o formatter/Prettier já cobre.
- 2 achados originalmente marcados `major` pela Pass 6 (bugs) foram rebaixados para `minor` na verificação desta consolidação — ver achados #3 e #4 acima para o motivo de cada rebaixamento.
- `<CODEBASE_DIR>` (`.makuco/docs/codebase/`) tem apenas `testing.md` — projeto greenfield, sem `OVERVIEW.md`/`conventions.md`/`architecture.md` ainda. A Pass 7 derivou os padrões de posicionamento observando a própria estrutura criada nesta PBI, já que ela é a primeira feature do projeto.
- Fan-out completo nesta rodada: 5 subagentes em paralelo (passes 3-7), mais passes 1-2 executadas pelo orquestrador em paralelo ao fan-out. Nenhuma pass foi `SKIP` ou `N/A`.

## Rodada de revisão 2 — 2026-09-07

**Veredicto:** NECESSITA CORREÇÕES
**Tasks revisadas:** T11 (habilitar CORS), T12 (rate limiting no endpoint de registro) — correções dos achados #1 e #2 da rodada 1

### Resumo

Revisão de rodada 2 (modo PBI), escopo incremental: 9 arquivos alterados desde a rodada 1 (commit `a98f1af`) até o HEAD atual (`f68d117`), cobrindo exatamente as correções de T11 (CORS) e T12 (rate limiting). 7 passes executadas (fan-out completo: 5 subagentes em paralelo para as passes 3-7, mais passes 1-2 no orquestrador), 9 achados após dedup/verificação/rollup (0 critical, 1 major, 6 minor, 2 suggestion), a partir de 16 candidatos brutos (15 dos 5 subagentes + 1 do orquestrador, descartado por ser observação de processo — ver Notas de escopo). Veredicto NECESSITA CORREÇÕES por 1 achado major novo, introduzido/exposto pela própria correção do achado #2: o `ThrottlerGuard` identifica clientes por `req.ip` sem `trust proxy` configurado, o que colapsaria todos os usuários reais em um único "balde" de rate limit caso o backend seja implantado atrás de qualquer reverse proxy/load balancer — topologia comum para deploys Node/Nest em produção.

### Rodada anterior (verificação dos achados bloqueantes da rodada 1)

| # (rodada 1) | Severidade | Status nesta rodada | Evidência |
| --- | --- | --- | --- |
| 1 | critical | **CORRIGIDO** | `services/backend/src/configure-app.ts` (L20-22) chama `app.enableCors({ origin: process.env.FRONTEND_URL ?? 'http://localhost:3000' })`; `src/main.ts` (L7) chama `configureApp(app)` antes de `app.listen`. Verificado estaticamente e empiricamente: a pass de segurança subiu a aplicação real e confirmou que uma `Origin` maliciosa (`http://evil.com`) não é refletida na resposta — a origem permanece fixa em `http://localhost:3000`. `test/cors.e2e-spec.ts` prova o header via Supertest. |
| 2 | major | **CORRIGIDO** | `services/backend/src/app.module.ts` (L19-25) registra `ThrottlerModule.forRoot([{ name: 'default', ttl: 60000, limit: 5 }])`, **não** como `APP_GUARD` global; `src/auth/auth.controller.ts` (L37-38) aplica `@UseGuards(ThrottlerGuard)` + `@Throttle(...)` só no endpoint `register`. Verificado estaticamente e empiricamente: a pass de segurança rodou a aplicação real e confirmou 429 na 7ª requisição em 60s, com outros endpoints não afetados. `test/auth-register-rate-limit.e2e-spec.ts` prova o 429 via Supertest. **Nota**: a correção em si está completa e correta; ela expõe, no entanto, uma lacuna de robustez nova (achado #1 desta rodada, abaixo) — que não invalida a correção do achado #2 original, mas bloqueia esta rodada por si mesma. |

### Achados (rodada 2)

| # | Severidade | Arquivo | Linha | Categoria | Descrição | Recomendação |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | major | `services/backend/src/app.module.ts` (L19-25) + `services/backend/src/configure-app.ts` / `src/main.ts` | — | segurança/robustez (OWASP A04) | **PLAUSIBLE.** `ThrottlerGuard` identifica o cliente por `req.ip` (comportamento padrão do `@nestjs/throttler`), mas nem `main.ts` nem `configure-app.ts` chamam `app.set('trust proxy', ...)`. Atrás de qualquer reverse proxy/load balancer (topologia comum de deploy Node/Nest — Nginx, PaaS, ALB, ingress; a própria documentação do `@nestjs/throttler` alerta especificamente para este caso), `req.ip` reflete o IP do proxy para TODAS as requisições: a partir da 6ª tentativa de registro de QUALQUER usuário em 60s, TODO usuário real passa a receber 429 — inclusive quem não fez nenhuma tentativa própria, um DoS parcial autoinfligido no fluxo de cadastro. Alternativamente, se `trust proxy` for ligado sem validar a cadeia de `X-Forwarded-For`, o limite passa a ser contornável forjando esse header. Nenhum teste cobre essa topologia — o e2e roda direto contra `httpServer()` em memória, sem proxy no meio. Depende da topologia de deploy real, ainda não definida no projeto (dev local apenas até aqui) — por isso `PLAUSIBLE`, não `CONFIRMED`. | Confirmar a topologia real de deploy; se houver proxy/LB à frente, chamar `app.set('trust proxy', <hops confiáveis>)` em `configure-app.ts` com o número exato de hops (nunca `true` genérico), documentando a decisão, e adicionar um teste simulando `X-Forwarded-For` que prove a resolução correta do IP do cliente real. |
| 2 | minor | `services/backend/src/app.module.ts` (L21-24) + `services/backend/src/auth/auth.controller.ts` (L38) | — | clean-code (DRY) | O limite (`5`) e a janela (`60000`) do rate limit estão hardcoded e duplicados entre `ThrottlerModule.forRoot(...)` e `@Throttle(...)`; nada garante que uma alteração futura em um lugar seja replicada no outro, divergindo silenciosamente do valor documentado nos comentários de ambos os arquivos. | Extrair para constantes nomeadas e compartilhadas (ex. `AUTH_RATE_LIMIT_MAX = 5`, `AUTH_RATE_LIMIT_WINDOW_MS = 60000`), ou remover o `@Throttle` redundante e deixar o endpoint herdar o `'default'` já registrado globalmente. |
| 3 | minor | `services/backend/test/cors.e2e-spec.ts` (L35-43) | — | cobertura | O teste do achado #1 (rodada 1) sempre roda pelo branch de fallback de `configure-app.ts` (`process.env.FRONTEND_URL ?? 'http://localhost:3000'`): nem o `.env` real do projeto nem o teste definem `FRONTEND_URL`, e o valor asserido é idêntico ao fallback — o branch em que a env var é de fato lida nunca é exercitado. Uma regressão que hardcodasse o valor e removesse a leitura da env var passaria despercebida; a cobertura de linha de `configure-app.ts` (excluída da métrica unitária conforme `testing.md`) é real, mas a de branch é parcial. | Definir `process.env.FRONTEND_URL` para um valor distinto do default (ex. `http://test-origin.example`) no `beforeAll`, e assertar esse valor — provando que a env var é lida, não só que o default funciona. |
| 4 | minor | `services/backend/src/configure-app.ts` (L20-22) | — | segurança (OWASP A05) | O fallback `process.env.FRONTEND_URL ?? 'http://localhost:3000'` é silencioso: se a env var não for definida em produção, o CORS passa a aceitar só `http://localhost:3000`, reproduzindo — por má configuração, de forma silenciosa — o mesmo sintoma do achado #1 original da rodada 1 (frontend real bloqueado pelo navegador). | Seguir o padrão já usado em `supabase.provider.ts` (`requireEnv`) e falhar rápido no bootstrap se `FRONTEND_URL` não estiver definida fora de desenvolvimento, em vez de cair num default silencioso. |
| 5 | minor | `services/backend/test/auth-register-rate-limit.e2e-spec.ts` (L56-61 e L72-87) | — | isolamento de teste | Nem o loop de tentativas nem o `afterEach` tratam falhas (erro de rede/timeout no meio do loop; `deleteUser` rejeitando no meio da limpeza) — qualquer falha isolada interrompe a limpeza dos IDs restantes, deixando usuários órfãos no projeto Supabase real usado pelos testes. Não afeta usuários finais (é código de teste), mas acumula débito operacional no projeto de teste a cada execução instável. | Envolver cada `deleteUser` (e a captura do ID criado) em `try/catch`/`Promise.allSettled`, garantindo que uma falha isolada não impeça a limpeza das demais contas criadas. |
| 6 | minor | `services/backend/src/configure-app.ts` (L18-23) | — | SOLID-S | `configureApp` mistura duas responsabilidades que evoluem por razões distintas (validação de payload e política de CORS) na mesma função — hoje pequeno, mas convida a crescer como um "bag" de configurações não relacionadas conforme mais setup global for adicionado. | Separar em `configureValidation(app)` e `configureCors(app)`, com `configureApp` apenas orquestrando as duas chamadas. |
| 7 | minor | `services/backend/test/cors.e2e-spec.ts` (L35-43) | — | cobertura | O teste só cobre um `GET /` simples (sem preflight); o fluxo real do achado #1 é um `POST /auth/register` com corpo JSON, que dispara preflight `OPTIONS`. Risco residual baixo — o middleware de CORS do Nest trata preflight automaticamente — mas o caminho não é provado por teste. | Adicionar um caso `OPTIONS /auth/register` com `Access-Control-Request-Method: POST` e o `Origin` do frontend, validando os headers de preflight. |
| 8 | suggestion | `services/backend/src/app.module.ts` (L19-25) | — | escalabilidade | `ThrottlerModule.forRoot` usa o storage padrão em memória, por instância do processo — aceitável para o escopo declarado desta PBI (MVP de uma única ONG, instância única); se o serviço escalar horizontalmente, o limite efetivo vira "5/min × nº de instâncias". | Migrar para um storage compartilhado (ex. Redis) antes de escalar horizontalmente. |
| 9 | suggestion | `services/backend/src/configure-app.ts` (L21) | — | clean-code | O literal `'http://localhost:3000'` (fallback de desenvolvimento) é repetido sem nome que expresse a intenção. | Extrair para uma constante, ex. `const DEFAULT_LOCAL_FRONTEND_URL = 'http://localhost:3000'`. |

### Cobertura dos critérios de aceite

Não aplicável no sentido usual — T11/T12 não implementam novos critérios REG-NN, corrigem achados de review (`Requirement: Achado #1/#2 de review.md rodada 1`). Os três itens de "Done when" de cada task foram verificados end-to-end (Pass 1): T11 (3/3 verificado) e T12 (3/3 verificado) — ver "Rodada anterior" acima para a evidência de código.

### Notas de escopo

- Fora de escopo de achado: `services/backend/package-lock.json` (lockfile).
- Passes executadas: 1-7, todas aplicáveis (nenhuma `N/A`/`SKIP`). Fan-out completo (5 subagentes em paralelo para passes 3-7), mais passes 1-2 no orquestrador em paralelo ao fan-out.
- Contexto de convenções ausente: `.makuco/docs/codebase/` só contém `testing.md` (sem `OVERVIEW.md`/`conventions.md`/`architecture.md`/`structure.md`) — mesma lacuna já observada na rodada 1. A pass 7 derivou os padrões diretamente do código-fonte (arquivos irmãos em `services/backend/src/auth/`, `services/backend/src/supabase/`, e os `test/*.e2e-spec.ts` já existentes), sem encontrar nenhuma violação de estrutura/nomenclatura/camadas.
- 1 candidato descartado por não ser achado sobre este diff (não entra na tabela): observação da pass 7 sobre a ausência de `conventions.md`/`architecture.md`/`structure.md` no projeto — é uma lacuna de processo já registrada acima em "Contexto de convenções ausente", não um achado sobre o código revisado.
- Ajustes de severidade feitos na consolidação (nenhum acima do que os subagentes reportaram, apenas abaixo, com justificativa): achado 1 desta rodada mantido `major` (evidência concreta e mecanismo bem documentado do `@nestjs/throttler`, apesar de uma pass tê-lo classificado como `suggestion`); achado 2 rebaixado de `major` (proposto por uma pass) para `minor` — é um risco futuro de duplicação, não um bug presente hoje; achado 3 rebaixado de `major` (proposto por uma pass) para `minor` — a correção de produção está correta, é a cobertura do teste que é parcial; o achado sobre usuários órfãos (5) rebaixado de `major` (proposto por uma pass) para `minor` — é código de teste, sem consequência para usuário final ou produção.

## Rodada de revisão 3 — 2026-09-07

**Veredicto:** NECESSITA CORREÇÕES
**Tasks revisadas:** T13 (documentar decisão de trust proxy) — correção do achado #1 da rodada 2

### Resumo

Revisão de rodada 3 (modo PBI), escopo incremental: 3 arquivos alterados desde a rodada 2 (commit `f68d117`) até o HEAD atual (`7f2136c`) — a correção de T13 (comentário de decisão + teste de regressão) mais um fix de configuração do Sonar descoberto durante o closeout (não relacionado ao achado #1, mas necessário para o Quality Gate passar). 7 passes executadas (fan-out completo), 6 achados após dedup/verificação (0 critical, 1 major, 3 minor, 2 suggestion), a partir de 9 candidatos brutos. Veredicto NECESSITA CORREÇÕES: o achado #1 da rodada 2 (trust proxy) está **corrigido** — via decisão de produto documentada e verificada, não uma mudança de comportamento, já que não existe hoje nenhuma topologia de deploy real no projeto — mas a rodada encontrou 1 achado major novo: o teste de regressão escrito para travar essa decisão (`test/trust-proxy.e2e-spec.ts`) não exercita de fato a função `configureApp()` onde a decisão vive, então não protege contra a regressão que deveria prevenir.

### Rodada anterior (verificação do achado bloqueante da rodada 2)

| # (rodada 2) | Severidade | Status nesta rodada | Evidência |
| --- | --- | --- | --- |
| 1 | major | **CORRIGIDO** | Decisão documentada em `services/backend/src/configure-app.ts` (comentário `[DECISÃO]`, L18-34) e em `.makuco/STATE.md` (seção Recent Decisions): `trust proxy` não é configurado hoje porque não existe nenhuma topologia de deploy real no projeto (confirmado por inspeção: sem Dockerfile, CI/CD, ou config de PaaS no repositório) — configurar um valor adivinhado abriria bypass via `X-Forwarded-For` forjado sem resolver o problema real. A decisão está registrada para ser revisitada quando a infraestrutura real for definida. `test/trust-proxy.e2e-spec.ts` prova que, no cenário atual (conexão direta), um `X-Forwarded-For` forjado e distinto por requisição não contorna o rate limit — 6 tentativas continuam bloqueadas na 6ª (429). **Ressalva**: o teste que prova isso tem uma falha de desenho (ver achado #1 desta rodada) que enfraquece sua capacidade de travar uma regressão futura — isso não reabre o achado #1 da rodada 2 (o comportamento de produção de hoje está correto e verificado), mas é tratado como um achado novo e bloqueante nesta rodada. |

### Achados (rodada 3)

| # | Severidade | Arquivo | Linha | Categoria | Descrição | Recomendação |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | major | `services/backend/test/trust-proxy.e2e-spec.ts` | `beforeAll` (~L46-53) | test validity | **CONFIRMED** (o defeito no teste) / **PLAUSIBLE** (a consequência de regressão futura). O teste nunca chama `configureApp(app)` — monta a app com `app.useGlobalPipes(new ValidationPipe())` manual e segue para `app.init()`, igual ao teste irmão `auth-register-rate-limit.e2e-spec.ts`, mas diferente de `test/cors.e2e-spec.ts` (citado no próprio comentário de `configure-app.ts` como o padrão a seguir, precisamente para que o teste rode contra a mesma configuração usada em produção). Resultado: o teste comprova o comportamento DEFAULT do Express/Nest (trust proxy desligado por padrão), não a configuração real que passa por `configureApp()`. Se um dev futuro adicionar `app.set('trust proxy', ...)` dentro de `configureApp()` — exatamente a regressão que a decisão documentada diz querer prevenir — esta suíte continua passando, porque a instância testada nunca passa por `configureApp()`. A decisão fica sem proteção real de regressão, apesar do teste alegar prová-la. | Importar `configureApp` de `../src/configure-app` e chamá-lo no `beforeAll` (mesmo ponto de `cors.e2e-spec.ts`, antes de `app.init()`), substituindo a chamada manual a `useGlobalPipes`. |
| 2 | minor | `services/backend/src/configure-app.ts` | L3-34 | clean-code (comentários) | Dois blocos `/** */` adjacentes acima de `configureApp` (um da rodada 1 sobre CORS, um novo desta rodada sobre trust proxy) — padrão não usado em nenhum outro lugar do projeto. Ferramentas cientes de JSDoc só reconhecem o bloco mais próximo da declaração como documentação da função; o primeiro bloco fica efetivamente órfão para essas ferramentas. | Mesclar os dois blocos em um único comentário JSDoc contínuo acima de `configureApp`, com uma seção por decisão. |
| 3 | minor | `services/backend/src/configure-app.ts` | L18-34 | consistência de convenção | O marcador `[DECISÃO]` usado nesta anotação não corresponde à convenção formal já reconhecida pelo processo do projeto (`SPEC_DEVIATION: [...] \ Reason: [...]`, verificada por tooling em `makuco-code-review`/`step-02-contexto.md`) nem ao estilo sem marcador usado no bloco de rodada 1 no mesmo arquivo — dois estilos diferentes para o mesmo propósito (achado de review documentado em comentário de código), nenhum alinhado à convenção formal. | Usar `SPEC_DEVIATION` (convenção já reconhecida e verificada por tooling) ou documentar `[DECISÃO]` formalmente como categoria válida em `step-02-contexto.md` — e aplicar a escolha retroativamente ao bloco da rodada 1 por consistência. |
| 4 | minor | `services/backend/test/trust-proxy.e2e-spec.ts` (~L79-99) | — | força de asserção | O teste só verifica que a 6ª tentativa retorna 429; não verifica que as 5 primeiras retornaram 201 (diferente do teste irmão `auth-register-rate-limit.e2e-spec.ts`, que faz esse check). Reduz a força da prova de que o IP forjado está sendo corretamente ignorado nas 5 primeiras tentativas, não só na 6ª. | Adicionar `expect(responses.slice(0, 5).every(r => r.status === 201)).toBe(true)` (ou equivalente). |
| 5 | suggestion | `services/backend/src/configure-app.ts` (L18-34) vs `test/trust-proxy.e2e-spec.ts` (L12-30) | — | duplicação de narrativa | O racional da decisão está escrito quase verbatim em dois lugares (comentário do código + docstring do teste); risco de divergirem se a decisão for revisitada só em um dos dois. | Manter a narrativa completa em um lugar (`configure-app.ts`, por ser o ponto de decisão) e referenciar brevemente no outro. |
| 6 | suggestion | `services/backend/src/configure-app.ts` / `.makuco/STATE.md` | — | documentação de risco residual | A decisão não menciona explicitamente o risco de um deploy real (ex.: um ambiente de demonstração ad-hoc num PaaS) introduzir um proxy antes da PBI de infraestrutura formalizar a topologia — o que reintroduziria silenciosamente o DoS funcional original (todos os usuários no mesmo balde de rate limit). Nenhum mecanismo (lint/CI/log) detectaria essa mudança de topologia. | Adicionar essa consequência ao comentário/`STATE.md` (ex.: "não fazer deploy deste backend atrás de proxy/LB antes de tratar esta decisão") e considerar logar um aviso quando `X-Forwarded-For` chegar numa requisição real com trust proxy desligado. |

### Cobertura dos critérios de aceite

Não aplicável no sentido usual — T13 não implementa um novo critério REG-NN, corrige o achado #1 (major) de `review.md` Rodada de revisão 2. Dos 3 itens de "Done when" de T13: os itens 1 (comentário de decisão) e 3 (registro em STATE.md) foram verificados end-to-end; o item 2 ("teste de integração comprova...") está **parcialmente satisfeito** — o teste existe e passa, mas não exercita a função `configureApp()` onde a decisão realmente vive (ver achado #1 desta rodada), então não comprova a decisão com a força que o item pretendia.

### Notas de escopo

- Fora de escopo de achado: nenhum arquivo excluído nesta rodada (todos os 3 arquivos alterados foram avaliados).
- Passes executadas: 1-7, todas aplicáveis. Fan-out completo (5 subagentes em paralelo para passes 3-7), mais passes 1-2 no orquestrador em paralelo ao fan-out.
- Contexto de convenções ausente: `.makuco/docs/codebase/` só contém `testing.md` — mesma lacuna já observada nas rodadas 1 e 2.
- Ajuste de severidade feito na consolidação: o achado #1 desta rodada foi reportado como `critical` por uma das passes (por analogia com o quanto o teste falha em proteger a decisão) — rebaixado para `major` na consolidação, porque o comportamento de produção atual está correto e verificado independentemente (não há exploração/bypass/crash acontecendo hoje); o risco real é a ausência de uma trava de regressão futura, o que corresponde à definição de `major` ("bug likely to reach production"), não de `critical`.
- Dedup: o achado sobre "teste não usa `configureApp()`" foi levantado por duas passes independentes (patterns, como suggestion; tests, como critical/major) — consolidado em um único achado (#1), mantendo a redação mais concreta (a da pass de testes) por nomear o cenário de regressão exato.
- Verificação empírica: a pass de segurança confirmou, por inspeção do repositório (ausência de Dockerfile/CI-CD/config de PaaS), que a premissa da decisão documentada (nenhuma topologia de deploy real hoje) é verdadeira — não é uma suposição não verificada.

---

Próximo passo: volte ao `makuco-desenvolver` para uma nova task corrigindo o achado #1 desta rodada (fazer `test/trust-proxy.e2e-spec.ts` chamar `configureApp(app)` em vez de montar a app manualmente) e rode uma nova rodada de review depois.
